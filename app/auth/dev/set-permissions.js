const setPermissions = async (request, organisationId) => {
  // for development, no need to set permissions
  console.log('Local development permissions set')
}

module.exports = {
  setPermissions
}
